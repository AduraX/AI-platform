# Eval Service

Offline and online evaluation orchestration for models, prompts, and retrieval quality.

Local run:

```bash
cd services/eval-service && uv run uvicorn eval_service.main:app --reload --port 8007
```
