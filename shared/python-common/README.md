# python-common

Shared Python package for configuration, auth helpers, schemas, database settings, and observability primitives.

Install via the workspace root with `uv sync --all-packages --group dev`.
