apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: enterprise-ai
  labels:
    app: ingestion-service
spec:
  selector:
    app: ingestion-service
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
  type: ClusterIP
