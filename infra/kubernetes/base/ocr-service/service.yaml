apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: enterprise-ai
  labels:
    app: ocr-service
spec:
  selector:
    app: ocr-service
  ports:
    - port: 8005
      targetPort: 8005
      protocol: TCP
  type: ClusterIP
