apiVersion: v1
kind: Service
metadata:
  name: model-router
  namespace: enterprise-ai
  labels:
    app: model-router
spec:
  selector:
    app: model-router
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
  type: ClusterIP
