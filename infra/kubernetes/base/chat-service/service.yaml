apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: enterprise-ai
  labels:
    app: chat-service
spec:
  selector:
    app: chat-service
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
  type: ClusterIP
