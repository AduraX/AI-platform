apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: enterprise-ai
  labels:
    app: rag-service
spec:
  selector:
    app: rag-service
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
  type: ClusterIP
